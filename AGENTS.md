# Workspace Rules for Vibentra

## 🔒 Scope Boundary & Authorization

1. **Frontend & Portfolio Customizations Authorized:**
   - Safely maintain `frontend/` in sync with `Vibentra_kotlin/web/`.
   - Per user directive on 2026-09-04, `Portfolio/` customizations are authorized.

2. **Strictly Protected Areas (DO NOT TOUCH):**
   - **`backend/`**: Do not modify any backend files or server logic.
   - **`api/`**: Must remain completely untouched.
   - Root configuration and server logic must remain safe.

